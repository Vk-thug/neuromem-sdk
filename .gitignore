# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
# These are anchored to the repo root with a leading slash so they
# don't match nested 'lib/' (e.g. ui/src/lib/) or 'build/' dirs.
.Python
/build/
develop-eggs/
/dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Database files
*.db
*.sqlite
*.sqlite3

# Logs
*.log

# Environment variables
.env
.env.local

# NeuroMem specific
neuromem.yaml.local

.claude
CLAUDE.md
plan/
live-test/
reference/

# Benchmark artifacts — dataset files + reproducible result JSONs
data/
benchmarks/results/
benchmarks/datasets/.cache/

# v0.4.2: SPA build output. CI builds this before `python -m build`;
# locally, run `cd ui && npm run build`. Keep the directory but ignore
# the generated bundle.
neuromem/ui/web/assets/
neuromem/ui/web/index.html
ui/node_modules/
ui/dist/
ui/tsconfig.tsbuildinfo