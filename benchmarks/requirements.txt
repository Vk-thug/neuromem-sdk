# NeuroMem Benchmark Suite - Dependencies
# Install: pip install -r benchmarks/requirements.txt

# NeuroMem SDK (install from local)
-e ..

# Competitor memory systems
mem0ai>=0.1.0
langmem>=0.0.10           # LangMem: pip install langmem
zep-cloud>=3.0.0          # Zep: requires ZEP_API_KEY env var

# LLM for judge evaluation
litellm>=1.80.0
ollama>=0.4.0

# Embeddings
openai>=1.30.0
langchain-openai>=0.3.0   # For LangMem embeddings

# Data handling
requests>=2.31.0
numpy>=1.24.0
python-dotenv>=1.0.0      # Auto-load .env for API keys

# Output formatting
tabulate>=0.9.0
rich>=13.0.0

# Latency measurement
statistics  # stdlib, listed for documentation
